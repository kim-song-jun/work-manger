<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- GPS for clock-in / location-based auto detect -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      Background location is required by the geofence path
      (lib/geofence/geofence_service.dart). Play Store will scrutinise this
      submission — provide a clear in-app explainer + a privacy-policy link.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Push (Android 13+ requires runtime grant) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Foreground service for future geofence / auto clock-in worker -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Foreground service holding the ntfy WebSocket alive across Doze. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:label="근무 관리"
        android:name="${applicationName}"
        android:icon="@drawable/ic_launcher"
        android:usesCleartextTraffic="true">

        <!-- Cleartext only allowed for 10.0.2.2 (emulator host) in dev builds.
             Prod manifests should remove usesCleartextTraffic above and
             provide a network_security_config.xml restricting it. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ntfy push transport — see ADR-006. The foreground service keeps
             the WebSocket alive across Doze; the SPA starts/stops it via
             the JS bridge. -->
        <service
            android:name=".push.NtfyForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ================== Home-screen widgets (Glance) ================== -->
        <!-- TodayStatusWidget: 출근/퇴근 상태 + 누적 근무 시간 -->
        <receiver
            android:name=".widget.TodayStatusWidgetReceiver"
            android:exported="true"
            android:label="@string/wm_widget_today_desc">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_status_widget_info" />
        </receiver>

        <!-- ThisWeekWidget: 주간 누적 시간 + 잔여 연차 -->
        <receiver
            android:name=".widget.ThisWeekWidgetReceiver"
            android:exported="true"
            android:label="@string/wm_widget_week_desc">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/this_week_widget_info" />
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
