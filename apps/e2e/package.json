{
  "name": "@work-manager/e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for the work-manager monorepo (web:4444 → api:4455).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "@types/node": "^20.11.0",
    "typescript": "^5.7.2"
  }
}
